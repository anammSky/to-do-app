const authRouter = require("./auth");
const seedRouter = require("./seed");
const tagRouter = require("./tag");
const taskRouter = require("./task");

module.exports = { authRouter, seedRouter, tagRouter, taskRouter };
